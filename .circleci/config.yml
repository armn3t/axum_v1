# This config was automatically generated from your source code
# Stacks detected: cicd:github-actions:.github/workflows,deps:rust:.
version: 2.1
jobs:

  test-rust:
    docker:
      - image: cimg/rust:1.73
    steps:
      - checkout
      - restore_cache:
          key: cargo-{{ checksum "Cargo.lock" }}
      - run:
          command: cargo check
      - setup_remote_docker
      - run:
          command: docker version
      - run:
          command: docker compose version
      - run:
          command: docker compose up && docker compose exec app diesel migration run && docker compose exec app cargo test
      # - run:
      #     command: docker compose exec app diesel migration run
      # - run:
      #     command: docker compose exec app cargo test
      - save_cache:
          key: cargo-{{ checksum "Cargo.lock" }}
          paths:
            - ~/.cargo
  deploy:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: deploy
          command: '#e.g. ./deploy.sh'
      - run:
          name: found github actions config
          command: ':'
workflows:
  build-and-test:
    jobs:
      - test-rust
    # - deploy:
    #     requires:
    #       - test-rust
